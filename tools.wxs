module.exports = {
  twoDecimal: function (num) {
    var result = parseFloat(num);
    if (isNaN(result)) {
      return 0;
    }
    result = Math.round(num * 10) / 10;
    var s_x = result.toString();
    return s_x;
  },
}